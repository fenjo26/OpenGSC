import test from "node:test";

// Placeholder so the registered glob (src/lib/push/*.test.ts) matches at least one file from
// the foundation commit. N10 replaces it with real payload/subscription-filter tests.
test("placeholder (N10)", () => {});

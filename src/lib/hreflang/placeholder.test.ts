import test from "node:test";

// Placeholder so the registered glob (src/lib/hreflang/*.test.ts) matches at least one
// file from the foundation commit. N1 replaces it with real generator tests.
test("placeholder (N1)", () => {});

import test from "node:test";

// Placeholder so the registered glob (src/lib/local/*.test.ts) matches at least one file from
// the foundation commit. N4 replaces it with real phone/NAP/schema/GBP tests.
test("placeholder (N4)", () => {});

import test from "node:test";

// Placeholder for the foundation commit (src/lib/indexing/serpIndex.test.ts is registered in
// test:unit on top of the broader src/lib/indexing/*.test.ts glob). N6 replaces it with real
// site:-parsing and URL-normalisation tests.
test("placeholder (N6)", () => {});

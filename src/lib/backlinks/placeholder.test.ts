import test from "node:test";

// Placeholder so the registered glob (src/lib/backlinks/*.test.ts) matches at least one
// file from the foundation commit. N2 replaces it with real toxicity/disavow/recovery tests.
test("placeholder (N2)", () => {});

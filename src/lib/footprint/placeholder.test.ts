import test from "node:test";

// Placeholder so the registered glob (src/lib/footprint/*.test.ts) matches at least one
// file from the foundation commit. N1 replaces it with real skeleton tests.
test("placeholder (N1)", () => {});

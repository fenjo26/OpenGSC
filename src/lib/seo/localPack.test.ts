import test from "node:test";

// Placeholder for the foundation commit (the glob src/lib/seo/localPack.test.ts is registered
// in test:unit). N3 replaces it with real matchLocalPack + provider-parsing tests.
test("placeholder (N3)", () => {});

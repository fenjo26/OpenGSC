import test from "node:test";

// Placeholder so the registered glob (src/lib/trends/*.test.ts) matches at least one file
// from the foundation commit. N5 replaces it with real growth-window/suggest tests.
test("placeholder (N5)", () => {});

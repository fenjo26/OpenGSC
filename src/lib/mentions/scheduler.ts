// T6 (docs/tasks/wave-oct/T6-mentions.md) owns this file. startMentionsScheduler /
// kickMentionsScheduler are deliberately EMPTY in this stub: instrumentation.ts calls them at
// server start, so a throw would take the whole instance down before T6 lands. The signatures
// are the wave contract (CONTRACT.md §3).

export function startMentionsScheduler(): void {}

export function kickMentionsScheduler(): void {}

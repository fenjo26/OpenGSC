import test from "node:test";

// Placeholder so the registered glob (src/lib/reports/*.test.ts) matches at least one file
// from the foundation commit. N8 replaces it with real schedule/render/section tests.
test("placeholder (N8)", () => {});

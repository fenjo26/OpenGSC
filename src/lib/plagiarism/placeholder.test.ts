import test from "node:test";

// Placeholder so the registered glob (src/lib/plagiarism/*.test.ts) matches at least one file
// from the foundation commit. N6 replaces it with real sampling/shingle tests.
test("placeholder (N6)", () => {});

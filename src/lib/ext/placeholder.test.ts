import test from "node:test";

// Placeholder so the registered glob (src/lib/ext/*.test.ts) matches at least one file from
// the foundation commit. N11 replaces it with real token/CORS/matching tests.
test("placeholder (N11)", () => {});

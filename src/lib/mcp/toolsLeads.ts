// MCP tools for widget leads (N9). Stub from the wave foundation (CONTRACT.md §3):
// N9 (docs/tasks/wave-nov/N9-audit-widget-leads.md) owns this file and fills the array
// (list_leads, local).

import { type McpTool } from "./shared";

export const LEADS_TOOLS: McpTool[] = [];

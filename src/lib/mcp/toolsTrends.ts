// MCP tools for the trend radar (N5). Stub from the wave foundation (CONTRACT.md §3):
// N5 (docs/tasks/wave-nov/N5-trend-radar.md) owns this file and fills the array
// (get_trends, local).

import { type McpTool } from "./shared";

export const TRENDS_TOOLS: McpTool[] = [];

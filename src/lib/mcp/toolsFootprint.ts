// MCP tools for network footprints (N1). Stub from the wave foundation (CONTRACT.md §3):
// N1 (docs/tasks/wave-nov/N1-footprint-hreflang.md) owns this file and fills the array
// (get_footprints, local).

import { type McpTool } from "./shared";

export const FOOTPRINT_TOOLS: McpTool[] = [];

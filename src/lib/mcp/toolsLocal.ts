// MCP tools for Local SEO (N4). Stub from the wave foundation (CONTRACT.md §3):
// N4 (docs/tasks/wave-nov/N4-local-seo.md) owns this file and fills the array
// (get_local_profile — local, check_nap — net).

import { type McpTool } from "./shared";

export const LOCAL_TOOLS: McpTool[] = [];

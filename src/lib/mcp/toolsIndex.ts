// MCP tools for automatic index checks (T4). Stub from the wave foundation (CONTRACT.md §5):
// T4 (docs/tasks/wave-oct/T4-index-autocheck.md) owns this file and fills the array.

import { type McpTool } from "./shared";

export const INDEX_TOOLS: McpTool[] = [];
